{
  "name": "fullstack-template",
  "private": true,
  "scripts": {
    "install:frontend": "pnpm install --dir frontend",
    "install:backend": "bun install --cwd backend",
    "install:all": "pnpm run install:frontend && pnpm run install:backend",
    "dev:frontend": "pnpm --dir frontend dev",
    "dev:backend": "bun run --cwd backend dev",
    "dev:db-studio": "bun run --cwd backend db:studio",
    "dev": "concurrently -k -n frontend,backend -c cyan,magenta \"pnpm run dev:frontend\" \"pnpm run dev:backend\" \"pnpm run dev:db-studio\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
